# SecureDrive 项目经验总结

## 一、项目概述

SecureDrive 是一个跨平台加密磁盘工具，基于 C++ + Dear ImGui + GLFW 构建，支持：
- 创建加密分区（格式化加密）
- 便携模式（U 盘双分区：明文 FAT32 + 加密分区）
- ~~免格式化加密（就地加密，保留原有文件）~~ — **已删除**
- VFS 文件管理器（解锁后像普通文件系统一样操作）

## 二、已完成的工作

### 1. 删除免格式化加密功能
**原因**：该功能涉及 Windows 卷设备卸载/重挂载、双句柄切换、XTS 加密、NTFS 解析、VFS 格式化等多个复杂环节的串联，任何一个环节出错就会导致整体失败。且 C++ 原生 GUI + 直接磁盘 I/O 的架构下，该功能天然就非常脆弱。

**教训**：
- 不要试图在一个已经有大量技术债务的代码库上叠加高风险功能
- 就地加密后解析 NTFS 是一个"自找麻烦"的设计，真实 NTFS 分区的复杂性远超简化解析器的能力
- 和 VeraCrypt/BitLocker 等知名工具保持一致的行为（加密后原有数据冻结，用户从备份手动恢复）是最务实的选择

### 2. 删除免格式化加密 UI 按钮
- `src/ui/app.cpp`：移除了 `🔐 免格式化加密（保留现有文件！）` 按钮
- 现在只有红色按钮 `[!] 卸载文件系统 → 初始化为加密分区（清空数据！）`

### 3. 修复界面 emoji 乱码
ImGui 默认字体不支持 Unicode emoji，导致界面上显示 `?` 乱码。

**修复方式**：将所有 emoji 替换为 ASCII 文本

| 原字符 | 替换为 |
|--------|--------|
| `⚠` | `[!]` |
| `🔐` | *(按钮已删除)* |
| `🔒` | `[锁定]` |
| `✓` | `[OK]` |
| `○` | `[-]` |
| `✗` | `[X]` |
| `🚀` | `[便携]` |

**教训**：在 C++ 原生 GUI 中使用 emoji 前，必须确认字体支持。ImGui 默认字体只包含基本 ASCII 和 CJK 字符，不支持彩色 emoji。

### 4. 日志重定向到文件
- 修改 `src/main.cpp`，启动时将 `stdout` 和 `stderr` 重定向到 `SecureDrive.log`
- 软件界面/终端不再显示任何诊断日志
- 每次启动自动写入时间戳分隔线

### 5. 隐藏控制台窗口
- `CMakeLists.txt`：设置 `SDRV_CONSOLE=OFF`，使用 `WIN32` 子系统
- 链接标志追加 `/ENTRY:mainCRTStartup`，让 Windows 子系统程序正确找到 `main()` 入口点
- `main.cpp`：保留 `FreeConsole()` 作为兜底

## 三、免格式化加密的反复失败总结

### 问题根因时间线

| 轮次 | 修了什么 | 为什么还是失败 |
|------|---------|--------------|
| 1 | 加 `partition_size` 参数 | 根本问题不是大小 |
| 2 | 加扇区对齐 | 不是对齐问题 |
| 3 | tail header 移到磁盘末尾 | 解决了写入位置问题 |
| 4 | fallback 到卷 GUID 路径 | 解决了打开设备问题 |
| 5 | `FILE_FLAG_NO_BUFFERING` | 解决了缓冲问题 |
| 6 | dismount+lock 重试 | 解决了卷锁问题 |
| 7 | `FSCTL_ALLOW_EXTENDED_DASD_IO` | 解决了末尾写入限制 |
| 8 | 改法A：`data_disk.close()` | 解决了双句柄冲突 |
| 9 | 修复 `NtfsBootSector` 偏移 | 解决了 MFT 位置读错 |
| 10 | 完整的备份/恢复方案 | 用户未看到备份界面，最终放弃 |

### 核心问题

1. **双句柄切换竞态条件**：`data_disk` 和 `disk_` 指向同一个卷设备，`data_disk` 析构释放卷锁的瞬间，Windows 会尝试重新挂载卷，导致 `disk_` 句柄失效
2. **NTFS Boot Sector 结构体偏移错误**：`unused_bpb` 字段大小错误（1 byte vs 2 bytes），导致后续所有字段偏移都错了 1 byte
3. **加密后解析 NTFS 的脆弱性**：24 分钟加密完成后，所有数据已被 XTS 加密，要用简化版 NtfsReader 正确解析 MFT、DataRun、FixUp 等复杂结构，面对真实数据时非常脆弱
4. **2GB 内存限制**：`MAX_MIGRATE_BYTES = 2GB`，大文件会被丢弃
5. **便携模式与就地加密的根本冲突**：便携模式会先重新分区，**重新分区本身就抹掉了所有数据**

### 关键教训

- **不要在加密后解析文件系统**：要么加密前用操作系统 API 备份（此时文件系统还是明文），要么就不保留文件
- **不要试图打补丁修复一个架构上有根本缺陷的功能**：每次修一个症状，遇到下一个症状，效率极低
- **远程调试没有实际运行环境时，必须要求用户提供完整日志**：不能只靠"软件崩溃了"这种模糊描述来推断
- **技术决策要尽早做**：如果发现某个功能实现难度远超预期，应该尽早和用户沟通，选择更务实的方案

## 四、技术决策反思

### 应该在一开始就问用户的问题

1. "你希望用户体验是什么样的？类似 VeraCrypt（加密后原有数据冻结，用户从备份手动恢复）还是一键无痛（加密后原有文件自动出现在 VFS 里）？"
2. "你测试的是便携模式还是就地加密？便携模式会先重新分区，数据会丢失"
3. "请提供完整的日志输出，特别是 `[BACKUP]` 开头的行"

### 应该在一开始就做对的事情

1. **简化就地加密**：直接加密 → 格式化空白 VFS → 完成。不解析 NTFS，不备份文件。
2. **明确区分两种模式**：
   - 便携模式：重新分区 → 创建空白加密分区 → 格式化 VFS
   - 普通模式：直接格式化加密分区
3. **如果坚持要自动保留文件**：加密前用 Windows API 扫描并备份到临时位置，加密后恢复到 VFS。而不是加密后解析 NTFS。

## 五、C++ 原生 GUI 开发的注意事项

1. **字体问题**：ImGui 默认字体不支持 emoji，使用 ASCII 替代符号
2. **子系统问题**：Windows GUI 程序需要 `WIN32` 子系统 + `/ENTRY:mainCRTStartup`
3. **日志管理**：原生 GUI 程序不应该依赖控制台输出，日志应写入文件
4. **管理员权限**：操作原始磁盘分区需要 UAC 提升，通过 manifest 实现

## 六、项目文件清单

### 修改的文件
- `CMakeLists.txt` — 关闭控制台窗口、修正链接入口点
- `src/main.cpp` — 日志重定向到文件、释放控制台窗口
- `src/ui/app.cpp` — 删除免格式化按钮、修复 emoji 乱码
- `src/ui/init_wizard.cpp` — 删除免格式化相关代码、修复 emoji 乱码、简化流程
- `src/ntfs/ntfs_reader.h` — 修复 NtfsBootSector 结构体偏移（虽然该功能已删除）

### 保留的功能
- ✅ 普通格式化加密
- ✅ 便携模式（U 盘双分区）
- ✅ VFS 文件管理器
- ✅ 密码解锁/锁定
- ✅ 紧急密码
- ✅ 错误次数限制 + 数据擦除

## 七、给用户的核心建议

1. **SecureDrive 的定位应该是"简洁可靠的加密磁盘工具"，而不是"万能数据迁移工具"**
2. **用户加密前自行备份重要文件是标准做法**，和 BitLocker/VeraCrypt 一致
3. **如果未来需要"自动保留文件"功能**，应该作为独立的外部工具实现（先备份到另一个磁盘，再加密，最后恢复），而不是集成到加密工具内部

---

*总结日期：2026-06-09*
